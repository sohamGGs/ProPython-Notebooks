import pyshorteners

# Replace with your actual token if you have one
# s = pyshorteners.Shortener(api_key='YOUR_BITLY_TOKEN')
# print(s.bitly.short('https://www.google.com'))

print("Note: Most services require an API key passed into the constructor.")